# Python build / runtime
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Dataset shards (large, regenerable from scripts)
data/

# Training outputs and caches
out/
supercomputer-out/
.multirun/
cache/
feature_cache/
tokenizer/

# Eval / wandb / logging
wandb/
logs/
experiment/fineweb_val_metrics_gpu*.json
experiment/*.bak
experiment/*.json.bak

# Sweep runtime state (yamls themselves are tracked)
sweeps/.active

# Workflow notes (claude-code workspace)
notes/

# Scratch / pruning area (orphan code parked here, not for the preprint)
scratch/

# Tooling artifacts
.claude/
core.*
*.zip
nohup.out

# Exploratory subprojects not in paper
interpretability/
dann/
token-stats-translation/
notebooks/
